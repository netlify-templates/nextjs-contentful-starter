# Postgres connection string. In production point this at a hosted Postgres
# instance (Neon, Supabase, Railway, RDS, ...).
DATABASE_URL="postgresql://user:password@localhost:5432/rappuarvio"

# NextAuth
# Generate a real secret with: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
