<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MEGA SORTEIOS - Rifa ECO PARQUE</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&display=swap');
        body { font-family: 'Poppins', sans-serif; }
        
        .number-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(60px, 1fr));
            gap: 8px;
        }
        
        .number-btn {
            transition: all 0.3s ease;
            transform: scale(1);
        }
        
        .number-btn:hover {
            transform: scale(1.05);
        }
        
        .number-btn.selected {
            animation: pulse 0.5s ease-in-out;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .eco-gradient {
            background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        }
        
        .floating {
            animation: float 3s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Header -->
    <div class="gradient-bg text-white py-8">
        <div class="container mx-auto px-4 text-center">
            <h1 class="text-4xl md:text-6xl font-bold mb-2 floating">🎯 MEGA SORTEIOS</h1>
            <p class="text-xl md:text-2xl opacity-90">Sua chance de ganhar está aqui!</p>
        </div>
    </div>

    <div class="container mx-auto px-4 py-8 max-w-6xl">
        <!-- Prize Info -->
        <div class="eco-gradient rounded-2xl p-8 mb-8 text-white text-center shadow-2xl">
            <div class="text-6xl mb-4">🏆</div>
            <h2 class="text-3xl md:text-4xl font-bold mb-4">PRÊMIO</h2>
            <div class="text-5xl md:text-6xl font-black mb-2">R$ 100,00</div>
            <p class="text-xl md:text-2xl opacity-90">EM PRODUTOS ECO PARQUE</p>
            <div class="mt-6 bg-white bg-opacity-20 rounded-xl p-4">
                <p class="text-lg font-semibold">📅 Data do Sorteio: EM BREVE</p>
            </div>
        </div>

        <!-- Info Cards -->
        <div class="grid md:grid-cols-3 gap-6 mb-8">
            <div class="bg-white rounded-xl p-6 shadow-lg text-center border-l-4 border-blue-500">
                <div class="text-3xl mb-3">💰</div>
                <h3 class="font-bold text-lg mb-2">Preço por Número</h3>
                <p class="text-2xl font-bold text-blue-600">R$ 3,00</p>
            </div>
            
            <div class="bg-white rounded-xl p-6 shadow-lg text-center border-l-4 border-green-500">
                <div class="text-3xl mb-3">📱</div>
                <h3 class="font-bold text-lg mb-2">Pagamento PIX</h3>
                <p class="text-lg font-bold text-green-600">14996033779</p>
            </div>
            
            <div class="bg-white rounded-xl p-6 shadow-lg text-center border-l-4 border-purple-500">
                <div class="text-3xl mb-3">🎲</div>
                <h3 class="font-bold text-lg mb-2">Total de Números</h3>
                <p class="text-2xl font-bold text-purple-600">100 números</p>
            </div>
        </div>

        <!-- Number Selection -->
        <div class="bg-white rounded-2xl p-6 shadow-xl mb-8">
            <h3 class="text-2xl font-bold text-center mb-6 text-gray-800">
                🎯 Escolha seus números da sorte (00 a 99)
            </h3>
            
            <div class="number-grid mb-6" id="numberGrid">
                <!-- Numbers will be generated by JavaScript -->
            </div>
            
            <div class="text-center">
                <div class="bg-gray-100 rounded-xl p-4 mb-4">
                    <p class="text-lg font-semibold text-gray-700">Números selecionados: <span id="selectedCount" class="text-blue-600 font-bold">0</span></p>
                    <p class="text-xl font-bold text-green-600">Total: R$ <span id="totalPrice">0,00</span></p>
                </div>
                
                <div id="selectedNumbers" class="mb-4 min-h-[40px] flex flex-wrap gap-2 justify-center">
                    <!-- Selected numbers will appear here -->
                </div>
                
                <button id="reserveBtn" class="bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-bold py-4 px-8 rounded-xl text-xl shadow-lg transform hover:scale-105 transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed" disabled>
                    💳 Reservar Números Selecionados
                </button>
            </div>
        </div>

        <!-- Payment Instructions -->
        <div id="paymentSection" class="bg-yellow-50 border-2 border-yellow-200 rounded-2xl p-6 mb-8 hidden">
            <h3 class="text-2xl font-bold text-center mb-4 text-yellow-800">
                📱 Instruções de Pagamento
            </h3>
            <div class="bg-white rounded-xl p-6 shadow-md">
                <div class="text-center mb-4">
                    <p class="text-lg font-semibold text-gray-700">Chave PIX:</p>
                    <div class="bg-gray-100 rounded-lg p-3 font-mono text-xl font-bold text-center">
                        14996033779
                    </div>
                    <button onclick="copyPix()" class="mt-2 bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold transition-colors">
                        📋 Copiar Chave PIX
                    </button>
                </div>
                
                <div class="border-t pt-4">
                    <h4 class="font-bold text-lg mb-2">⚠️ Importante:</h4>
                    <ul class="list-disc list-inside space-y-1 text-gray-700">
                        <li>Após o pagamento, envie o comprovante via WhatsApp</li>
                        <li>Seus números serão confirmados após a verificação do pagamento</li>
                        <li>Guarde bem seus números escolhidos</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Rules -->
        <div class="bg-white rounded-2xl p-6 shadow-xl">
            <h3 class="text-2xl font-bold text-center mb-6 text-gray-800">📋 Regulamento</h3>
            <div class="grid md:grid-cols-2 gap-6">
                <div>
                    <h4 class="font-bold text-lg mb-3 text-blue-600">🎯 Como Participar:</h4>
                    <ul class="space-y-2 text-gray-700">
                        <li>• Escolha seus números de 00 a 99</li>
                        <li>• Cada número custa R$ 3,00</li>
                        <li>• Faça o pagamento via PIX</li>
                        <li>• Envie o comprovante</li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-bold text-lg mb-3 text-green-600">🏆 Premiação:</h4>
                    <ul class="space-y-2 text-gray-700">
                        <li>• Prêmio: R$ 100,00 em produtos ECO PARQUE</li>
                        <li>• Data do sorteio: EM BREVE</li>
                        <li>• Sorteio será transmitido ao vivo</li>
                        <li>• Ganhador será contactado imediatamente</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="gradient-bg text-white py-8 mt-12">
        <div class="container mx-auto px-4 text-center">
            <h3 class="text-2xl font-bold mb-2">🎯 MEGA SORTEIOS</h3>
            <p class="opacity-90">Sua sorte está a um clique de distância!</p>
            <p class="mt-4 text-sm opacity-75">PIX: 14996033779 | Produtos ECO PARQUE</p>
        </div>
    </footer>

    <script>
        let selectedNumbers = new Set();
        const pricePerNumber = 3.00;

        // Generate number grid
        function generateNumberGrid() {
            const grid = document.getElementById('numberGrid');
            for (let i = 0; i <= 99; i++) {
                const numberStr = i.toString().padStart(2, '0');
                const button = document.createElement('button');
                button.className = 'number-btn bg-white hover:bg-blue-100 border-2 border-gray-300 hover:border-blue-400 rounded-lg p-3 font-bold text-lg transition-all duration-300 shadow-sm hover:shadow-md';
                button.textContent = numberStr;
                button.onclick = () => toggleNumber(numberStr, button);
                grid.appendChild(button);
            }
        }

        // Toggle number selection
        function toggleNumber(number, button) {
            if (selectedNumbers.has(number)) {
                selectedNumbers.delete(number);
                button.className = 'number-btn bg-white hover:bg-blue-100 border-2 border-gray-300 hover:border-blue-400 rounded-lg p-3 font-bold text-lg transition-all duration-300 shadow-sm hover:shadow-md';
            } else {
                selectedNumbers.add(number);
                button.className = 'number-btn selected bg-gradient-to-r from-blue-500 to-blue-600 text-white border-2 border-blue-600 rounded-lg p-3 font-bold text-lg transition-all duration-300 shadow-md';
            }
            updateSelection();
        }

        // Update selection display
        function updateSelection() {
            const count = selectedNumbers.size;
            const total = count * pricePerNumber;
            
            document.getElementById('selectedCount').textContent = count;
            document.getElementById('totalPrice').textContent = total.toFixed(2).replace('.', ',');
            
            const reserveBtn = document.getElementById('reserveBtn');
            reserveBtn.disabled = count === 0;
            
            // Display selected numbers
            const selectedDiv = document.getElementById('selectedNumbers');
            selectedDiv.innerHTML = '';
            
            if (count > 0) {
                const sortedNumbers = Array.from(selectedNumbers).sort((a, b) => parseInt(a) - parseInt(b));
                sortedNumbers.forEach(number => {
                    const span = document.createElement('span');
                    span.className = 'bg-blue-100 text-blue-800 px-3 py-1 rounded-full font-bold';
                    span.textContent = number;
                    selectedDiv.appendChild(span);
                });
            } else {
                selectedDiv.innerHTML = '<p class="text-gray-500 italic">Nenhum número selecionado</p>';
            }
        }

        // Reserve numbers
        document.getElementById('reserveBtn').onclick = function() {
            if (selectedNumbers.size === 0) return;
            
            document.getElementById('paymentSection').classList.remove('hidden');
            document.getElementById('paymentSection').scrollIntoView({ behavior: 'smooth' });
            
            // Show success message
            const button = this;
            const originalText = button.innerHTML;
            button.innerHTML = '✅ Números Reservados!';
            button.className = 'bg-gradient-to-r from-green-500 to-green-600 text-white font-bold py-4 px-8 rounded-xl text-xl shadow-lg';
            
            setTimeout(() => {
                button.innerHTML = originalText;
                button.className = 'bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-bold py-4 px-8 rounded-xl text-xl shadow-lg transform hover:scale-105 transition-all duration-300';
            }, 3000);
        };

        // Copy PIX key
        function copyPix() {
            const pixKey = '14996033779';
            navigator.clipboard.writeText(pixKey).then(() => {
                const button = event.target;
                const originalText = button.innerHTML;
                button.innerHTML = '✅ Copiado!';
                button.className = 'mt-2 bg-green-500 text-white px-4 py-2 rounded-lg font-semibold';
                
                setTimeout(() => {
                    button.innerHTML = originalText;
                    button.className = 'mt-2 bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold transition-colors';
                }, 2000);
            });
        }

        // Initialize
        generateNumberGrid();
        updateSelection();
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'9754278465a988fb',t:'MTc1NjIxOTk2MS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
